# 2026-04-14

## 数据库隔离+主从同步修复
- 用户要求执行Redis「数据库隔离+主从同步」方案，因为笔耕和总助串号了
- **根因**：笔耕容器缺少 AGENT_ID 环境变量，默认回退到 "master"，导致：
  - 与总助共用 chat key（chat:master:xxx）
  - 两个Agent互相干扰消息处理
  - Redis数据混淆
- **修复步骤**：
  1. agent.js 补丁：`process.env.AGENT_ID || "master"` → `process.env.AGENT_ID || process.env.AGENT_ROLE || "master"`（兜底逻辑）
  2. 重建 gzh 镜像（docker-compose build --no-cache gzh）
  3. docker-compose up -d gzh 重建容器（AGENT_ID=gzh 正确设置）
- **附带问题修复**：
  - OrcaTerm AI 设置了 Redis 密码 `MediaMatrix2024!`，导致所有容器 NOAUTH 报错
  - 移除了 Redis 密码（内网不需要）
  - Redis 容器意外被删除，手动重建（`docker run --name redis --network-alias redis`）
  - Redis 新容器需要 `--network-alias redis` 才能让其他容器通过 hostname "redis" 连接
- **验证结果**：
  - 总助：🎯 总助 (master)，AGENT_ID=master ✅
  - 笔耕：✍️ 笔耕 (gzh)，AGENT_ID=gzh ✅
  - Chat key 隔离：master 2条 / gzh 2条 ✅

## 笔耕创作任务
- 派了拼豆工具主题给笔耕：Claude+MiniMax M2做小红书爆火的拼豆工具
- Pipeline 执行成功：
  - ✅ Claude 写文案
  - ✅ Seedream 生成3张图
  - ✅ 微信公众号草稿箱发布
  - ❌ 最后回复消息报错 chatId is not defined（代码bug，需修复）
- 修复 chatId bug：agent.js:1191 行 fullPipelinePublish 函数引用了未定义的 chatId

## 系统状态（18:38）
- Redis: 新容器运行正常（PONG），无密码
- 总助: running, AGENT_ID=master
- 笔耕: running, AGENT_ID=gzh
- 抖灵: running
- 海拓: running
- Nginx: running

## 教训
- Redis 容器重建时必须带 `--network-alias redis`，否则其他容器无法通过 "redis" hostname 连接
- OrcaTerm AI 可能在无人监督时修改 Redis 配置（加了密码），需要检查
- docker-compose 有 ContainerConfig bug，老容器可能无法 recreate，需手动 docker rm + docker run


- 20:43 Redis密码问题又出现了！所有agent(gzh/douyin/overseas)都在crash loop，报错"ERR Protocol error: unauthenticated multibulk length"
  - 根因：redis.conf里有`requirepass MediaMatrix2024!`，但agent代码没配密码，连接Redis时被拒
  - 修复：注释掉redis.conf里的requirepass行，重启Redis和所有容器
  - 同时修复了agent-master（Redis重启导致SocketClosedUnexpectedlyError崩溃）
  - SSH密钥在session重启时丢失，重新生成并让用户部署公钥
  - 已把SSH密钥备份到workspace/ssh-backup/防止再丢
  - 教训：Redis密码问题反复出现，需要一劳永逸——要么全配密码要么全不配，不能混着来


- 21:00 晚报接龙已发出 ✅
- 小红书5张卡片图+文案生成完毕（腾讯IMA主题），已写入多维表格 ✅
- 用户确认：展示墙+语音播报配置开完会再做
- 今日核心问题总结：Redis密码反复被设导致agent全挂、Triage评分太严(全B/C)不派活、SSH密钥重启丢失


- 海媒矩阵品牌落地页创建完成（妙搭），暗色科技感主题+紫蓝渐变+毛玻璃卡片，路线图式产品线展示+作品展示+语音播报+合作入口+CTA
  - 链接：https://miaoda.feishu.cn/app/app_4jxvvpz0m10mh
  - 参考 CoolAi (cool-ai.team) 风格结构

# 2026-04-14

- Redis密码问题再次爆发！所有agent crash loop；注释掉requirepass+重启修复；SSH密钥备份到workspace
- 用户反馈笔耕15次不回复，定位为Redis密码问题导致
- 笔耕XHS Pipeline JSON解析失败（LLM输出中文混入JSON），降级逻辑已有但标题格式错误（"选题：xxx"而非人类可读标题）
- 多维表格内容创作表问题：
  - 45条记录中大量重复（Android照片位置×5、软件团队经济学×6）
  - 标题格式错误（"选题：英文原文"而非中文标题）
  - 内容摘要字段存了HTML代码和JSON字符串
  - 小红书笔记只有少量，且部分摘要为"(LLM调用失败)"
- 用户要求出笔耕工作日SOP，已创建 skills/content-quality-guide/笔耕工作日SOP.md
- 海媒矩阵品牌落地页已生成（app_4jxvvpz0m10mh），暗色科技感+紫蓝渐变+毛玻璃卡片+路线图结构
- 落地页需发布才能外访，已触发发布操作
- 用户期望：展示+交易+吸引资源的综合平台，不只是展示墙


- 笔耕工作日SOP v1.0注入agent-base/agent.js并重建容器生效
  - 每日5个创作时间节点（7:00/8:00/12:00/13:30/17:00）
  - 公众号8步流程：查重→搜素材→拟标题→写正文→自检→生图→发草稿箱→入表
  - 小红书4步流程：查重→publish_xhs→降级手动执行→发群
  - 关键规则：查重必做、标题必须中文可读、摘要纯文本、入库必做、草稿箱必发
- 发现笔耕之前3个问题：重复创作严重（同选题4-6遍）、公众号没发草稿箱、小红书没入库
- 教训：docker compose build context是agent-base/不是agents/gzh/，之前改错文件了
- Redis密码问题再次爆发（被设了MediaMatrix2024!），注释掉+重启修复
- 品牌落地页已上线（妙搭生成），参考CoolAi风格
- 用户希望展示墙+语音播报功能，待后续配置


- Redis任务订阅可靠性修复完成
  - dispatchTask: 加LPUSH到`queue:{agent}:tasks`，双通道（publish即时+LPUSH持久化）
  - start(): 加BRPOP阻塞消费+启动时pending任务恢复
  - 效果：容器重启后不丢任务，pending任务自动恢复执行
  - 总助和笔耕均已重建生效，日志确认BRPOP监听正常
- 笔耕恢复了一个之前丢失的dispatch任务（arxiv论文选题）
