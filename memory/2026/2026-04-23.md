# 2026-04-23 技术探索摘要

## 一、OpenCode + Ollama 对接状态

### OpenCode 容器状态
- **容器**：运行正常（Up 7 days），数据库迁移已完成
- **Web UI**：http://43.159.169.205:4000/（端口直连），密码 `opencode2024`
- **Nginx代理**：https://43.159.169.205/opencode/（已配置）
- **卷挂载**：
  - `/home/opencode/.config/opencode` — 配置目录
  - `/home/opencode/.local/share/opencode` — 数据目录
  - `/home/opencode/workspace` — 工作区

### Ollama 对接分析
- **OpenCode容器内**：无 Ollama（`command -v ollama` 无输出）
- **广州服务器**：`mediamatrix-ollama` 容器运行中（Up 2 weeks）
  - 广州地址：175.178.34.31
  - 两台服务器之间SSH连接需要解决密钥问题（见下）
- **当前架构问题**：OpenCode（硅谷）和 Ollama（广州）在不同服务器，需要网络打通

### 待解决：SSH密钥恢复
workspace 目录文件系统强制 777 权限，导致 `chmod 600` 无法生效，SSH密钥无法正常使用。临时解决：将密钥复制到 `/tmp` 绕过限制（本次探索已用此方案连通）。

**建议**：永久解决需要在服务器端添加当前SSH公钥，或使用 SSH Agent Forwarding。

### 对接路径建议
1. **方案A（推荐）**：在硅谷服务器上安装 Ollama（`docker run ollama/ollama`），与 OpenCode 同主机直接对接，无需跨服务器
2. **方案B**：配置 FRP 隧道打通两台服务器
3. **方案C**：OpenCode 直接调用 SiliconFlow/Claude 等云端 API（无需本地 Ollama）

---

## 二、Futurepedia 新工具探索（2026-04-17~22）

### 字节跳动系（高度相关）
| 工具 | 时间 | 类型 | 说明 |
|------|------|------|------|
| **Seedream 2.0 API** | 04/17 | 视频生成 | 多模态电影级视频生成API，豆包系 |
| **Seedream 5.0 AI Model** | 04/13 | 图像生成 | 高级图像创建与渲染，豆包系 |
| **Seedance 2.0 API** | 04/17 | 视频生成 | 电影级质量多模态视频，ByteDance |
| **Seed Diffusion Preview** | 10/17 | 文本生成 | 推理速度提升5倍 |

### 妙搭调性匹配工具（可入素材库）
| 工具 | 时间 | 说明 |
|------|------|------|
| **Augment Code** | trending | AI编程助手，代码补全/重构 |
| **STORYD** | Deep Dive | AI快速生成PPT演示文稿 |
| **HubSpot AEO** | 新功能 | AI搜索品牌可见性分析 |

### Anthropic 新动态
- **Claude Opus 4.7**：04/17上线，编码+视觉能力大幅提升，新安全防护
- **Claude Design**：AI可视化设计与原型工具
- **Claude Code Security**：AI代码漏洞检测与修复

### Nvidia × OpenClaw
- **OpenClaw on Jetson Thor**：边缘AI机器人本地运行（04/09）
- **Gemma 4 + OpenClaw 兼容**：本地Agent框架

### 结论
字节系（Seedream/Seedance）视频生成工具与妙搭AI兜空间高度相关，可作为视频能力扩展参考。编程和机器人工具（Hailuo/Owen方向）可入技术素材库。

---

## 三、SSH密钥问题记录

**问题**：workspace 目录文件系统强制 777，导致 SSH 私钥权限无法修改
**临时解决**：`cp key /tmp && chmod 600 /tmp/key`
**永久方案**：在服务器 `~/.ssh/authorized_keys` 添加公钥，或使用 SSH Agent



## 2026-04-23 下午

### 自动化运维体系完成

- ✅ Aily SSH 密钥已部署到两台服务器（`~/.ssh/id_ed25519_aily`，600权限）
- ✅ Aily 可直连两台服务器免密操作
- ✅ 服务器端脚本已上传到 `/root/scripts/`（agent-watchdog.sh / auto-deploy.sh / install-cron.sh）
- ✅ 看门狗 cron 已安装（开机自启 + 每30分钟检查一次）
- ✅ 本地管理脚本：`server-ops.sh`（主入口）、`daily_inspect.sh`、`container_ops.sh`
- ✅ **旭日正式退出日常运维**：SSH 连不上的技术故障才找旭日，其他全由 Aily 自动处理

### 两台服务器现有定时任务（crontab）

**广州（175.178.34.31）：**
- 看门狗（开机自启 + 每30分钟）
- @reboot `nohup bash /root/scripts/agent-watchdog.sh`
- `*/30 * * * *` `bash /root/scripts/agent-watchdog.sh`

**硅谷（43.159.169.205）：**
- 看门狗（开机自启 + 每30分钟）
- @reboot `nohup bash /root/scripts/agent-watchdog.sh`
- `*/30 * * * *` `bash /root/scripts/agent-watchdog.sh`
- OpenClaw海外信息抓取（HackerNews每2h / TechCrunch每2h / ProductHunt每4h）

### 待完成
- 海拓 agent.js 开发（广州新建 `/root/mediamatrix/agents/haituo/`）



### 海拓 agent 部署完成（2026-04-23 11:28）
- 广州路径：`/root/mediamatrix/agents/haituo/haituo.js`
- PM2管理：`pm2 list` / `pm2 logs haituo` / `pm2 restart haituo`
- 开机自启：PM2 startup 已配置
- 定时：每2小时自动执行一次主循环
- 能力：HackerNews抓取 + SiliconFlow速测生成 + 抖灵质量监控 + 飞书日报推送
- 群发：oc_08b9f4cbaecdbecc1837ab353080cc7c（海媒总群）
- 日志：`/root/mediamatrix/logs/haituo/haituo.log`

# 2026-04-23

## AI兜空间修复

### 任务背景
外部客户咨询GEO时需要展示妙搭作品墙，两个展示墙页面之前返回444错误。

### 修复结果
1. **AI播报作品集**：app_4jzu7xwvn1c3d → 已修复，新增今日小红书案例
2. **Portfolio AI案例展**：app_4jzubf2qpbgnh → 已修复，新增今日小红书案例，累计收录13+

### 今日新增小红书内容
- 标题：通勤5分钟搞定一上午的活！AI效率神器太绝了
- 内容：ChatGPT/Claude/Kimi 通勤场景使用技巧

### 页面风格
- Morandi莫兰迪风格保持完整
- 暖白米色背景 #F5F3EF
- 雾霾蓝绿色 #8EANB8
- 清新杂志风排版

### 链接
- AI播报作品集：https://miaoda.feishu.cn/app/app_4jzu7xwvn1c3d
- Portfolio AI案例展：https://miaoda.feishu.cn/app/app_4jzubf2qpbgnh


## 18:30 晚间盘点播报完成
- 飞书卡片已发（oc_1e07a0ec770fe1b0c4364f8d36944ad3）
- TTS语音：豆包音频API无响应（API Key可能不支持语音生成）
- 多维表格AI播报已更新（rowsInserted:1）
- 明日晨报初稿已写入：memory/2026-04-24-morning-brief-draft.md
- 今日信息池A选题：通义千问Qwen3、Kimi K2.6；B选题：Apple安全漏洞、AI电力危机
